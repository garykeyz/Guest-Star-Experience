const $=s=>document.querySelector(s),channel=new BroadcastChannel('guest-star-player-beta'),esc=v=>String(v||'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));function render(s={}){const x=s.settings||{},tracks=s.tracks||[];$('#hotel-title').textContent=x.hotel||'Guest Star Experience';$('#logo-fallback').textContent=(x.hotel||'GS').split(' ').map(a=>a[0]).join('').slice(0,2);$('#hotel-logo').hidden=!x.logo;if(x.logo)$('#hotel-logo').src=x.logo;const q=$('#qr');q.innerHTML='';if(x.requestUrl){const code=qrcode(0,'M');code.addData(x.requestUrl);code.make();q.innerHTML=code.createImgTag(7,8)}else q.textContent='Configura el enlace de solicitud en Host';const active=tracks.filter(t=>t.status==='ready'||t.status==='playing');$('#lobby-queue').innerHTML=active.map((t,i)=>`<li class="${t.id===s.currentId?'live':''}"><b>#${i+1}</b><div><strong>${esc(t.singer||'Próximo cantante')}</strong><small>${esc(t.name)}</small></div></li>`).join('')||'<li class="empty">¡Sé el primero en cantar! Escanea el QR.</li>';const now=tracks.find(t=>t.id===s.currentId);$('#now-title').textContent=now?.name||'Música de fondo';$('#now-singer').textContent=now?.singer||'La próxima canción aparecerá aquí.';const video=$('#lobby-video');if(s.mediaType?.startsWith('video/')&&s.mediaUrl&&video.src!==s.mediaUrl){video.src=s.mediaUrl;video.play().catch(()=>{})}if(!s.mediaType?.startsWith('video/')){video.pause();video.removeAttribute('src')}}channel.onmessage=e=>render(e.data);try{render(JSON.parse(localStorage.getItem('guest-star-player-lobby')))}catch{render()}$('#fullscreen').onclick=()=>document.documentElement.requestFullscreen?.();
